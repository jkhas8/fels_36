<% provide(:title, "New word") %>
<h1>New word</h1>

<div>
  <div>
    <%= form_for [:admin, @category, @word] do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "Create word", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
