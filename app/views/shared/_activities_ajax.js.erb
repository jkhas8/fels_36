$('.activities').append("<%= escape_javascript(render @activities) %>");
$('a.load-more').attr('href', "<%= root_path page: @activities.next_page %>");

<% unless @activities.next_page %>
  $('a.load-more').remove();
<% end %>
